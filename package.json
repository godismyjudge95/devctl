{
  "name": "devctl-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Test tooling for devctl — BATS integration tests and Playwright e2e tests",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "bats": "^1.11.1"
  }
}
